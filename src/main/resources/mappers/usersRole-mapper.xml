<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UsersRole">
	<!-- 개인별 추가 권한 목록 조회 -->
    <select id="getUserRoles" resultType="String">
        SELECT AUTH_GROUP FROM USERS_ROLE WHERE USERS_ID = #{users_id}
    </select>
    
    <insert id="insertUserRole">
	    INSERT INTO USERS_ROLE (ROLE_SEQ, USERS_ID, AUTH_GROUP)
	    VALUES (ROLE_SEQ.NEXTVAL, #{users_id}, #{auth_group})
	</insert>
	
	<delete id="deleteAllByUsersId">
	    DELETE FROM USERS_ROLE WHERE USERS_ID = #{users_id}
	</delete>
</mapper>
