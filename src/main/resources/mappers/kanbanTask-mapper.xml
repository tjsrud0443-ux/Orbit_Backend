<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="KanbanTask">
	
	<select id="getKanbanTaskList" resultType="com.study.app.domains.projects.KanbanTaskDTO">
		select k.*, u.name, u.sysname
			from kanban_task k
			left join users u on u.id = k.users_c_id
			where k.project_seq = #{project_seq}
			order by k.status, k.position
	</select>
	
	<select id="getProjectMembers" resultType="com.study.app.domains.projects.ProjectMembersDTO">
		select p.users_id, u.name, u.sysname
			from project_members p
			left join users u on u.id = p.users_id
			where p.project_seq = #{project_seq}
	</select>
	
	<select id="getNextPosition" resultType="Long">
	    select nvl(max(position), -1) + 1
		    from kanban_task
		    where project_seq = #{project_seq}
		    and status = #{status}
	</select>
	
	<insert id="insertTask">
		insert into kanban_task values(task_seq.nextval, #{project_seq}, #{title}, #{content}, #{status}, 
		#{priority}, #{users_pic_id}, #{start_date}, #{due_date}, #{position}, #{users_c_id}, sysdate, null)
	</insert>
</mapper>